<?php

namespace MediaWiki\Extension\ArticleSummaries;

use MediaWiki\Title\Title;

class SimpleSummaryChecker {
	/**
	 * Check if the given page has simple summaries available
	 * @param Title $title The page to check
	 * @return bool Whether summaries are available
	 */
	public static function hasSimpleSummaries( Title $title ) {
		// Placeholder implementation
		return true;
	}
}
